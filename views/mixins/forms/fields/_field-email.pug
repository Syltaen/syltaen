//- =============================================================================
//- > MIXIN : FORM FIELD - EMAIL
//- =============================================================================

mixin field-email(form, name, label = false, field_attributes = false)

    +fieldset($form, $name, $label)&attributes($attributes)
        input(type="email" name=$name value=$form["prefill"]->get($name))&attributes($field_attributes)
        block