//- =============================================================================
//- > MIXIN : FORM FIELD - TEXT
//- =============================================================================

mixin field-text(form, name, label = false, field_attributes = false)

    +fieldset($form, $name, $label)&attributes($attributes)
        input(type="text" name=$name placeholder=$placeholder value=$form["prefill"]->get($name))&attributes($field_attributes)
        block